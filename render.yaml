services:
  - type: web
    name: borneo-app-backend
    env: node
    buildCommand: npm install --prefix backend --legacy-peer-deps
    startCommand: npm start --prefix backend
    envVars:
      - key: NODE_ENV
        value: production
